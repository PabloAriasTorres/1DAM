/*Crea un programa que, para los números entre el 10 y el 20 (ambos
incluidos) diga si son divisibles entre 5, si son divisibles entre 6 y si son divisibles
entre 7, usando dos bucles anidados.*/
using System;
class Ejercicio_2253
{
	static void Main()
	{
		int dividir1, dividir2, dividir3;
		for(int n=10; n<=20; n++)
		{
			dividir1=n%5;
			dividir2=n%6;
			dividir3=n%7;
			for(n=10; n<=20; n++)
			{
				
			}
		}
	}
}
